{
    "name": "date-highlighter",
    "version": "1.0.0",
    "description": "Chrome extension for date conversion",
    "main": "background.js",
    "scripts": {
        "test": "mocha tests/**/*.test.js",
        "test:watch": "mocha tests/**/*.test.js --watch",
        "clean": "rm -rf dist",
        "build": "npm run clean && mkdir -p dist && zip -r dist/date-highlighter.zip . -x '*.git*' '*node_modules*' '*tests*' '*dist*' '*.DS_Store' 'package-lock.json'"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "mocha": "^10.8.2"
    }
}
